# EqvimechINVENTORY

One-time commands to connect a local repository to GitHub and push the `main` branch:

```bash
git remote add origin https://github.com/arabianfarhan/EqvimechINVENTORY.git
git branch -M main
git push -u origin main
```
